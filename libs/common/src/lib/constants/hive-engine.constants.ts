export const HIVE_ENGINE_CONTRACT = Object.freeze({
  TOKENS: 'tokens',
} as const);

export const HIVE_ENGINE_ACTION = Object.freeze({
  STAKE: 'stake',
  DELEGATE: 'delegate',
} as const);

export const HIVE_ENGINE_TOKEN = Object.freeze({
  WAIV: 'WAIV',
} as const);
