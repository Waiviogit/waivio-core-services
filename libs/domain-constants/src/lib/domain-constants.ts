/**
 * Domain constants library for Waivio object/field types and business rules.
 * Add Waivio-specific constants here (e.g. object types, field names, supposed updates).
 * Keep @waivio-core-services/common generic (no domain-specific constants).
 */

// Placeholder until object.constants and related domain constants are migrated from common
export const DOMAIN_CONSTANTS_LIB = 'domain-constants' as const;
